'use client'

/**
 * Legacy wrapper — newsletter body styles live in globals.css for specificity.
 * Kept so newsletter layout does not need to change.
 */
export function NewsletterStyles() {
  return null
}
